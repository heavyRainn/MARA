<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <application>
        <service
            android:name="com.care.voice.platform.android.reminder.ReminderVoiceForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name="com.care.voice.data.reminder.ReminderReceiver"
            android:exported="false" />

        <receiver
            android:name="com.care.voice.data.reminder.ReminderActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.care.voice.action.REMINDER_COMPLETE" />
                <action android:name="com.care.voice.action.REMINDER_SNOOZE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.care.voice.data.reminder.BootCompletedReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
